import React from 'react'

const FooterList = ({text,className}) => {
  return (
    <div>
      <li className={`font-normal font-pop text-primary text-base`}>{text}</li>
    </div>
  )
}

export default FooterList
